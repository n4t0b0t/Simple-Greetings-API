version: 2.0
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
  test:
    docker:
      - image: alpine:3.7
    steps:
      - run:
          name: Run unit tests
          command: npm test

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
